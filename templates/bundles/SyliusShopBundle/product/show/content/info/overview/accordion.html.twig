{# This file is intentionally left blank. 
   We are hiding the default accordion from the left column (overview hook) 
   because we are manually injecting it into the right column (summary block) 
   to match the Cateco custom layout. #}
